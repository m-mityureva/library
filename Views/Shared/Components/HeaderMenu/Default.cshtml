@using Microsoft.AspNetCore.Authorization
@inject IAuthorizationService AuthorizationService

<div user-identity="@User.Identity" class="form-inline text-white"></div>

<div class="form-inline text-white">
    <!--<div class="dropdown mr-2 col">
        @if (User.Identity.IsAuthenticated)
        {
            <a class="text-white dropdown-toggle" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                @User.Identity.Name
            </a>

            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                @if (User.IsInRole("Reader"))
                {
                    <a class=" dropdown-item" asp-action="UserList" asp-controller="Book" asp-route-returnUrl="/">My Books</a>
                    <a class=" dropdown-item" asp-action="UserList" asp-controller="ReturnRequest" asp-route-returnUrl="/"> My Return requests</a>
                }
                @if (User.IsInRole("Librarian") || User.IsInRole("Admin"))
                {
                    <a class=" dropdown-item" asp-action="List" asp-controller="Order" asp-route-returnUrl="/">Orders</a>
                    <a class=" dropdown-item" asp-action="List" asp-controller="ReturnRequest" asp-route-returnUrl="/">Return requests</a>

                }
                @if (User.IsInRole("Storekeeper") || User.IsInRole("Admin"))
                {
                    <a class=" dropdown-item" asp-action="Index" asp-controller="Admin" asp-route-returnUrl="/">All Books</a>

                }
            </div>
        }
    </div>


    <div class="text-right">
        @if (!User.Identity.IsAuthenticated)
        {
            <a class="text-white" asp-action="Login" asp-controller="Account" asp-route-returnUrl="/">Log In</a>
            <span> | </span>
            <a class="text-white" asp-action="Register" asp-controller="Account" asp-route-returnUrl="/">Sign Up</a>
        }
        else
        {
            <a class="text-white" asp-action="Logout" asp-controller="Account" asp-route-returnUrl="/">Log Out</a>
        }
    </div>
    -->
</div>
